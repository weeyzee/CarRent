export interface Car {
    carId: number;
    brand: string;
    model: string;
    licensePlate: string;
    status: string; // Статус может быть, например, "available", "booked", "in trip" и т.д.
  }
  